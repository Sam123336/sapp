<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10"> 

        <h5 class="mb-5">You can create a new post</h5>
        <form action="/update/<%= post._id %>" method="POST">
            <textarea placeholder="What's on your mind" name="content" class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%=post.content%></textarea>
            <input type="submit" class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2" value="Create new post">
        </form>
        
    </div>
</body>
</html>
